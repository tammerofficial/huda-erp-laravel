# 🚀 WooCommerce - البدء السريع

## ✅ تم التجهيز! النظام جاهز للاتصال بـ WooCommerce

---

## 📝 الخطوات المطلوبة منك:

### 1️⃣ احصل على API Keys من WooCommerce:

**اذهب إلى موقعك:**
```
WooCommerce → Settings → Advanced → REST API → Add Key
```

**انسخ:**
- ✅ Consumer Key (يبدأ بـ `ck_`)
- ✅ Consumer Secret (يبدأ بـ `cs_`)

---

### 2️⃣ حدّث ملف .env:

افتح الملف:
```bash
nano .env
```

**ابحث عن السطور دي وحدّثها:**
```env
WOOCOMMERCE_STORE_URL=https://موقعك.com/
WOOCOMMERCE_CONSUMER_KEY=ck_المفتاح_الحقيقي_هنا
WOOCOMMERCE_CONSUMER_SECRET=cs_المفتاح_السري_هنا
```

احفظ الملف: `Ctrl+O` ثم `Enter` ثم `Ctrl+X`

---

### 3️⃣ شغّل الـ Integration:

**طريقة سريعة (بسكريبت جاهز):**
```bash
./woocommerce-start.sh
```

**أو يدوياً:**
```bash
# اختبار الاتصال
php artisan woocommerce:sync

# تشغيل الـ Scheduler (sync كل 5 دقائق)
php artisan schedule:work
```

---

## 🎯 ماذا سيحدث؟

### كل 5 دقائق سيتم:

1. ✅ **استيراد العملاء** من WooCommerce
2. ✅ **استيراد المنتجات** من WooCommerce
3. ✅ **استيراد الطلبات** بحالة "On-Hold"
4. ✅ **حساب التكاليف** تلقائياً:
   - تكلفة المواد
   - تكلفة العمالة
   - تكلفة التشغيل
   - تكلفة الشحن (حسب البلد)
5. ✅ **حساب هامش الربح** %

---

## 📊 شاهد النتائج:

**الطلبات:**
```
http://127.0.0.1:8000/orders
```

**تقرير الأرباح:**
```
http://127.0.0.1:8000/reports/profitability
```

**تفاصيل تكلفة طلب معين:**
```
http://127.0.0.1:8000/orders/{id}/cost-breakdown
```

---

## ⚡ أوامر سريعة:

```bash
# Sync يدوي (لو عايز تجرب فوراً)
php artisan woocommerce:sync

# Sync تلقائي كل 5 دقائق
php artisan schedule:work

# إعادة حساب تكاليف كل المنتجات
php artisan products:recalculate-costs

# شوف الـ scheduled tasks
php artisan schedule:list
```

---

## 🔍 لو حصلت مشكلة:

**شوف الـ logs:**
```bash
tail -f storage/logs/laravel.log
```

**تأكد من الإعدادات:**
```bash
cat .env | grep WOO
```

---

## 📚 للمزيد من التفاصيل:

اقرأ الدليل الكامل:
```
WOOCOMMERCE_SETUP_AR.md
```

---

## ✅ جاهز؟

بمجرد تحديث بيانات WooCommerce في `.env`، شغّل:

```bash
./woocommerce-start.sh
```

وكل حاجة هتشتغل تلقائياً! 🎉

